{% extends 'base.html.twig' %}

{% block title %}Tristan{% endblock %}

{% block body %}
    <h1>Tristan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tristan.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tristan.name }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ tristan.lastname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tristan_index') }}">back to list</a>

    <a href="{{ path('app_tristan_edit', {'id': tristan.id}) }}">edit</a>

    {{ include('tristan/_delete_form.html.twig') }}
{% endblock %}
